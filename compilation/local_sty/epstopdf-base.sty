\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{epstopdf-base}[2009/07/12 Minimal local fallback]

% Minimal fallback package for environments missing epstopdf-base.
% This document uses PDF figures directly, so no EPS conversion hooks are needed.

\endinput
